import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Microgrid Digital Twin | Energy Scheduler",
  description: "Simulate 24-hour microgrid energy cycles with Solar, Battery, and Grid integration. Compare baseline vs smart scheduling strategies for optimal cost savings.",
  keywords: ["microgrid", "energy", "simulation", "solar", "battery", "digital twin", "peak shaving"],
  authors: [{ name: "Virtual Labs Hackathon" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-slate-950 text-white`}
      >
        {children}
      </body>
    </html>
  );
}
